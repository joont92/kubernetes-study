apiVersion: v1
kind: Pod
metadata:
  name: fortune
spec:
  containers:
  - image: joont92/fortune
    name: html-generator
    volumeMounts:
    - name: html
      mountPath: /var/htdocs
  - image: nginx:alpine
    name: web-browser
    volumeMounts:
    - name: html
      mountPath: /var/share/nginx/html
      readOnly: true
    ports:
    - containerPort: 80
      protocol: TCP
  volumes:
  - name: html
    emptyDir: {}