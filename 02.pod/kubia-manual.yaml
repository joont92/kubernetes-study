apiVersion: v1
kind: Pod
metadata:
  name: kubia-manual
spec:
  containers:
  - image: luksa/kubia
    name: kubia
    ports:
    - containerPort: 8080 # 그냥 외부에 알리는 용도. 방화병을 의미하는건 아니라고 
      protocol: TCP
